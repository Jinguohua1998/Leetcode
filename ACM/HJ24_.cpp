#include <bits/stdc++.h>

using namespace std;

// 本质上还是求两个递增子序列的长度（一个递增，一个递减）（动态规划）

int main(){
    int n;
    cin >> n;
    while(n--){
        int m;
        cin >> m;
        cout << m << endl;
    }
}