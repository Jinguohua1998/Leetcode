查询一个具体的值时，while(left < right)
查询最大/最小值，while(left <= right)
                mid = (left + right) / 2  right = mid ; left = mid - 1
                mid = (left + right + 1) / 2 right = mid + 1 ; left = mid